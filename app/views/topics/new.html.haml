- if flash[:notice] 
  %h3.notice
    = flash[:notice]

= form_for @topic do |f| 
  = render :partial => 'form', locals: { f: f } 
  %p.btn
    = submit_tag "Create"
or 
= link_to 'Cancel', :action => 'index' 
